/*global define */
define(function () {

    var PanelManager = function() {

        var panels = [];

        var add = function(panel) {
            panels.push(panel);
        };

        var showPanel = function(index) {

        };

        var hidePanel = function(index) {

        };
    };
    return PanelManager;
});